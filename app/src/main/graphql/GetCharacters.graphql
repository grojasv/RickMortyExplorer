query CharactersQuery {
    characters(page: 1) {
        results {
            id
            name
            gender
            origin {
                name
            }
            image
        }
    }
}